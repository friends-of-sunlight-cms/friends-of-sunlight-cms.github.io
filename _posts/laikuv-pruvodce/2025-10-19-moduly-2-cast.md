---
title: "Moduly - 2. èást"
author: "Golfin"
toc: true
toc_sticky: true
categories:
  - laikuv-pruvodce
tags:
  - laikuv-pruvodce
  - navody
  - moduly
---

### O modulech všeobecnì
Položili jste si nìkdy otázku, kolik vlastnì Sunlight používá modulù? Jestli ne, tak ta otázka má svoji platnost. Øeknìme si tedy, co je to ``modul``. Je to nìjaká nabídka, menu v administraèním rozhraní. V [prvním dílu ](https://friends-of-sunlight-cms.github.io/laikuv-pruvodce/moduly-1-cast/) jsme si ukázali, jak vytvoøit položku menu v hlavní administraèní lištì. Ale když se podíváme do menu ``Ostatní``, v hlavní lištì, vidíme tam talaèítka s ikonkami. To je také modul, tedy nìjaké menu. Dále, když se podíváme do ``Správy obsahu``, zde popravé stranì také vidíme nìjaká tlaèítka s ikonkami. Tak to jsou také menu moduly. Dnes si vyzkoušíme vytvoøit takové tlaèítko v záložce ``Ostatní``.


### Co všechno mùže obsahovat pole modulù?
Než se pustíme do práce, pøipomeneme si jeden soubor php, o kterém jsem psal v [prvním dílu ](https://friends-of-sunlight-cms.github.io/laikuv-pruvodce/moduly-1-cast/). Je to soubor modules.php ``\vaše doména\admin\modules.php``. Vysvìtlíme si nyní, co znamenají jednotlivé položky pole modulù. Tyto informace se netýkají pouze vytváøení tlaèítka nìjakého menu v hlavní nabídce ``Ostatní``, ale všech modulù, které v Sunlight vytvoøíme. To znamená, platí i pro pøedchozí díl návodu.

Toto je výòatek ze souboru ``modules.php``:

```php
/*
    Array format:

    array(
        module-name => array(
            title                   => module title
            access                  => allow access to current user 1/0

            [script]                => custom path to module script (defaults to "module/*name*.php", false = none)
            [url]                   => custom module URL (defaults to one generated by Router::admin())
            [parent]                => parent module name
            [custom_header] (0)     => disable title and backlink rendering 1/0

            [menu] (false)          => show module in main menu 1/0
            [menu_order] (15)       => main menu order

            [other] (false)         => show module in "other functions" module 1/0
            [other_order] (0)       => order in list of "other functions"
            [other_system] (0)      => show as system module in "other functions" 1/0
            [other_icon]            => path to button icon for this module in "other functions"
        ),
        ...
    )
*/
```

## Vysvìtlení jedotlivých položek pole (``Array`` - v php znamená pole) modulù:

``module-name``	- toto je název danného modulu. V základu vidíme v souboru ``modules.php``, že to mùže být Index (první hlavní stránka v administraèním menu), nebo content (To je ``Správa obsahu``), nebo users (``Uživatelé a skupiny``) a tak podobnì až po other (``Ostatní``).
``title`` - název modulu (menu), který se pøekládá do všech jazykù. Aby byla vidìt napø. ``Správa obsahu``, použijeme php kód ``_lang('admin.menu.content')``
``access`` - Pøístupová práva, zda toto menu uvidíme, nebo ne. Zde si všimnìme jedné variace. U modulu ``Index`` je zápis ``True``. Znamená to, že každý pøihlášený uživatel SL s pøístupem do administrace vždy uvidí Hlavní stránku tohoto rozhraní. Mohlo by tam být také ``false``, to by pak uživatel nevidìl. Ale zápis mùže vypadat i jinak. Pro modul ``Content`` nebo další je použit kód ``User::hasPrivilege('admincontent')``. V tom je SL úžasný redakèní systém, protože si mùžeme nastavit, jaká uživatelská role bude mít pøístup do danných položek, extendù a tak podobnì. V tomto pøípadì role ``admincontent`` umožní pøístup so ``Správy obsahu``.
``script`` - Všimnìme si, že v souboru ``modules.php`` pole modulù neobsahuje tuto položku. To proto, že není potøeba odkazovat na zvláštní výkonný skript php. Ale napøíklad v našich pokusech extendù už tuto položku používáme. Není to nic jiného, než nadefinovanná cesta k výkonnému skriptu php, který danný modul ovládá a pracuje s ním. Kód zápisu mùže vypadat napøíklad takto:  ``__DIR__ . '../../../script/content_first_menu.php'``. Soubor ``content_first_menu.php`` je vlastní výkonný skript php, který pracuje za nás.
``url`` - Php je jazyk, který umožòuje psát odkazy zástupnì. Snažím se to nyní vysvìtlit laicky. Když budu v html odkazovat na nìjaký soubor, napø. ``modul.html``, napíši to pøesnì takto. Aby internetový prohlížeš takový soubor našel a zobrazil. Ale v php se to dá napsat i jinak. A to je obsahem tohoto pole. Využívá tøídu Router, aby SL zobrazil danou cestu k nìjakému skriptu. Když do url zadám kód Router::admin('content-first-menu'), v adresním øádku prohlížeèe uvidíme cestu v tomto formátu: http://localhost/admin/index.php?p=content-first-menu. A prohlížeè ví a vidí kam má smìøovat a obsah ``content-first-menu`` zobrazí.
``parent`` - Toto není nic jiného, než ``zpìtný odkaz``. Klikneme si na nìjaké menu, tlaèítko, nìkam se dostaneme. Ale chceme zpátky. K tomu slouží link ``< návrat zpìt`` vlevo nahoøe. A položka ``parent`` nám to umožní.
``custom_header`` - Tato položka povolí nebo zakáže renderování ``názvu`` stránky, nebo ``backlinku`` (zpìtného odkazu). Pro zápis použijeme ``True`` nebo ``false``.
``menu`` - Povolí nebo zakáže zobrazení ``modulu`` (menu) v ``hlavní administraèní lištì``. Opìt použijeme ``True`` nebo ``false``. Všimnìme si, že hlavní položka, napø. ``Content`` má povoleno menu zobrazit. Ale jeho podmenu, napø. ``content-sort`` už položku menu neobsahuje. Není tam potøeba. Mi nechceme aby se ``Øazení`` stránek zobrazilo, jako hlavní položka administraèního menu lišty.
``menu_order`` - Poøadí jednotlivých ``modulù`` zobrazených ``z leva doprava``. ``Index`` má nulu, ``Content`` deset a tak dále. Nemusíme použít jen celé desítkové formáty èísel, ale i napøíklad 25, 34 a tak dále.
``other`` - Other, tedy èesky Ostatní. Zde prostì zapneme nebo vypneme zobrazení obsahu nìjakého extendu nebo menu v záložce ``Ostatní``. Opìt pomocí zápisu True nebo false.
``other_order`` - Tak jako u ``menu_order`` bylo poøadí v rámci celé administraèní lišty, tak zde to je poøadí menu vrámci pouze nabídky ``Ostatní``.
``other_system`` - Povolí, nebo zakáže zobrazení modulu jako ``systémový`` v nabídce ``Ostatní``. Opìt pomocí zápisu True nebo false. Zde musím podotknout, že v základu SL jsou všechny moduly v nabídce ``Ostatní`` vedeny jako systémové. Osobnì jsem nepotøeboval tuto funkci používat.
``other_icon`` - Krásná položka pole modulù v nabídce Ostatní, která vám vylepší vzhled tlaèítka vloženou ikonou. Zápis pro tuto položku mùže vypadat napø. takto: ``Router::path('admin/public/images/icons/big-broom.png')``. A nebo takto: ``Router::file(__DIR__ . '/../public/images/icons/big-broom.png')``.

Tak to je laické vysvìtlení jednotlivých položek modulù. Platí pro všechny zpùsoby použití modulù.

### Ukázkový extend pro zobrazení modulu v záložce Ostatní

Je to stejné, jako v [prvním dílu ](https://friends-of-sunlight-cms.github.io/laikuv-pruvodce/moduly-1-cast/) o modulech. Vytvoøme si tedy extend složku s názvem ``examplemodul-part2``. Opìt si v ní vytvoøíme soubor plugin.json a vyplníme podobnì, jako v minulém díle. Opìt pøipomínám èlánek od Jirky Daòka [Anatomie pluginù ](https://friends-of-sunlight-cms.github.io/serie/plugin-tutorial/anatomie-pluginu/).

```json
{
    "$schema": "../../../system/schema/extend.json",
    "name": "Example modul Part 2",
    "description": "Example show Modul - Other admin menu",
    "authors": [
        {"name": "Golfin", "url": "https://github.com/Helwa-Surinen/"}
    ],
    "version": "1.0.0",
    "environment": {
        "system": "^8.3.1"
    },
    "langs": {
        "modulother": "lang"
    },
    "events.admin": [
        {"event": "admin.init", "script": "event/admin/admin_init.php"}
    ]
}
```
### Rozdílnost v plugin.json

``name`` Jiné pojmenování extendu, v tomto pøípadì Example modul Part 2
``description`` JIný popis pluginu
``langs`` Klíè langs má jiný název. Pokud by byl stejný, jako u prvního dílu, tyto dva extendy by se zaèali hádat a systém by vyhazoval chyby.

### lang

Pak si opìt vytvoøíme novou složku ``lang``, kam vložíme jazykové soubory pro plugin. V ``plugin.json`` jsme uvedli, že pro tyto language øetìzce budeme používat klíè ``modulother``.
Ve složce lang si opìt vytvoøíme soubory ``cs.php`` a ``en.php``. Do nich vložíme následující kód pro cs:
```php
<?php

return [
    // admin
	'module.title.other' => 'Položka v menu Ostatní',
];
```

A pro anglickou verzi:

```php
<?php

return [
    // admin
	'module.title.other' => 'Example Other menu',
];
```
### event
Opìt v našem pluginu vytvoøíme složku ``event``. Ve, které si vytvoøíme další složku s názvem ``admin``.
A v ní vytvoøíme nový php soubor ``admin_init.php``. Pozor, tento php skript musíme zaregistrovat v ``plugin.json``. A to následujícím zpùsobem (mi to tam už máme):
```json
    "events.admin": [
        {"event": "admin.init", "script": "event/admin/admin_init.php"}
    ]
```
Vidíte, že nám tuto eventu odkazuje do složky ``admin`` na soubor ``admin_init.php``.

### Vlastní skript, který zobrazí menu v záložce Ostatní
Je to takøka stejné, jako u prvního dílu o modulech. Jen s malými nuancemi.
```php
<?php

use Sunlight\Router;													// odkazujeme na tøídu Router pro zobrazení ikony tlaèítka menu
use Sunlight\User;														// odkazujeme na tøídu user z dùvodu acces práv

return function (array $args) {
    $args['admin']->modules['example-modul-other'] = [					// example-modul-other se objeví v adresním øádku prohlížeèe
        'title' => _lang('modulother.module.title.other'),					// Název menu v záložce Ostatní
        'parent' => 'other',											// Parent ukazuje, že se má naše menu zobrazit v záložce Ostatní, když je povoleno
        'access' => User::hasPrivilege('adminother'),					// pøístupová práva do záložky Ostatní
        'script' => __DIR__ . '../../../script/example-modul-other.php',// cesta k výkonnému skriptu
        'other' => true,												// True povolí zobrazení menu, false zakáže zobrazení menu v záložce Ostatní
        'other_icon' => Router::file(__DIR__ . '../../../public/images/icons/big-cog.png') // nastavená cesta k ikonì tlaèítka menu
    ];
};
```

### Složky script a public

Všimli jste si (i u prvního dílu), že v koøenové složce extendu je další adresáø ``script`` a dnes také ``public``?
``script`` Tento adresáø obsahuje nìjaký php skript, který zobrazí obsah nabídky z Other menu. Vlastnì to výkonné a to co chceme aby dìlalo.
``public`` V tomto pøípadì adresáø public používáme pro veøejnì pøístupné prvky. Mi si zde vytvoøíme složku s názvem ``images``, ve které vytvoøíme další složku ``icons``. Do té vložíme obrázek ikony pro naše menu Other. Viz zápis v souboru admin-init 

```php
'other_icon' => Router::file(__DIR__ . '../../../public/images/icons/big-cog.png') // nastavená cesta k ikonì tlaèítka menu
```

### htaccess

Pokud takto spustíme stránky a mrkneme do záložky Ostatní, tlaèítko, které jsme si naskriptovali tam uvidéme. Ale pozor, neuvidíme ikonu tlaèítka. A to øeší soubor ``.htaccess``. Tento soubor umístíme do adresáøe ``public``. A v nìm bude následující kód:

```
<IfModule mod_authz_core.c>
    Require all granted
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Allow from all
</IfModule>
```

Více o této problematice se doèteme v dokumentaci SL [Podpora .htaccess](https://sunlight-cms.cz/dokumentace/podpora-htaccess/) a [Public adresáø](https://sunlight-cms.cz/dokumentace/pluginy/extend#public/).


Když tedy máme ošetøen soubor .htaccess, ikonka v tlaèítku se už ukáže.
![Hlavní Menu](/assets/posts/mdl_other_menu.jpg)

Jak vidíte, je to v zásadì zcela stejný postup, jak vytvoøit extend. jen s malou korekcí. V tom je CMS Sunlight úžasný systém. Samozøejmì ukázkový extend si mùžete opìt stáhnout u mì na gitu: [ExampleModul](https://github.com/Helwa-Surinen/Sunligt-CMS-laikuv-pruvodce/tree/main/examplemodul-part2).